// where you would point a client to talk to a homeserver
export const HOMESERVER_URL = 'https://chat.livingutopia.org'

// see https://t2bot.io/docs/access_tokens
export const ACCESS_TOKEN = ''

export const HTTP_LISTEN_PORT = 2020

// limited by `rc_invites` in synapses `homeserver.yaml` 
export const INVITE_RATE_PER_SECOND = 1